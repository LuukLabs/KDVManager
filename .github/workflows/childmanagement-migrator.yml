name: childmanagement-migrator

on:
  workflow_dispatch:
  push:
    branches:
    - master

    paths:
    - src/Services/ChildManagement/KDVManager.Services.ChildManagement.Persistence/**
    - .github/workflows/childmanagement-migrator.yml
  
  pull_request:
    branches:
    - master

    paths:
    - src/Services/ChildManagement/KDVManager.Services.ChildManagement.Persistence/**
    - .github/workflows/childmanagement-migrator.yml
env:
  SERVICE: childmanagement-migrator
  IMAGE: childmanagement-migrator

jobs:

  BuildContainersForPR_Linux:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'pull_request' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - uses: ./.github/workflows/composite/build
      with:
        service: ${{ env.SERVICE }}
        registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}

  BuildLinux:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'pull_request' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - uses: ./.github/workflows/composite/build-push
      with:
        service: ${{ env.SERVICE }}
        registry_host: ${{ secrets.REGISTRY_HOST }}
        registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}
        image_name: ${{ env.IMAGE }}
        registry_username: ${{ secrets.REGISTRY_USERNAME }}
        registry_password: ${{ secrets.REGISTRY_PASSWORD }}