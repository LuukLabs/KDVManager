apiVersion: v1
kind: Service
metadata:
  name: crm-api
  namespace: kdvmanager-prod
  labels:
    app: crm-api
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: crm-api
