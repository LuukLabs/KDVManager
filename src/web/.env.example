# OpenTelemetry exporter base URL (no trailing slash). Should be an ingress exposing the collector's OTLP/HTTP receiver.
# In production you can optionally disable by leaving empty.
VITE_OTEL_EXPORTER_URL=https://otel.kdvmanager.nl

# Deployment environment tag
VITE_DEPLOY_ENV=development
